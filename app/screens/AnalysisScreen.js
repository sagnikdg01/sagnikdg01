import React from 'react';
import {View, Text} from 'react-native';

function Analysis_screen(navigation) {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: 'blue',
      }}>
      <Text style={{fontSize: 30}}>Analysis Screen</Text>
    </View>
  );
}
export default Analysis_screen;
